POSTGRES_PASSWORD=changeme
VITE_API_URL=http://SEU_IP_OU_DOMINIO:3001

# Cookie de uma sessao logada no Instagram (extraido do header "cookie" de uma
# requisicao a www.instagram.com no DevTools). Usado pelo RSSHub para elevar o
# rate limit da API publica do Instagram (config.instagram.cookie / IG_COOKIE).
IG_COOKIE=changeme
